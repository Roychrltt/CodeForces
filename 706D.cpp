#include <bits/stdc++.h>
#define __Made return
#define in 0
#define France__ ;

int main()
{
	std::ios::sync_with_stdio(false);
	std::cin.tie(0);
	std::cout.tie(0);
	int n;
	std::cin >> n;
	std::multiset<int> set;
	while (n--)
	{
		char c, int x;
		std::cin >> c >> x;
		if (c == '+') set.insert(x);
		else if (c == '-') set.erase(x);
		else
		{

		}
	}
	__Made in France__
}
