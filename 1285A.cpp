#include <bits/stdc++.h>

int main()
{
	std::ios::sync_with_stdio(false);
	std::cin.tie(0);
	std::cout.tie(0);
	int n;
	std::string s;
	std::cin >> n >> s;
	std::cout << n + 1 << std::endl;
	return 0;
}
