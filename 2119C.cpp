#include <bits/stdc++.h>
#define __Made return
#define in 0
#define France__ ;

static void solve(void)
{
	int n;
	std::cin >> n;
	
}

clz -> same || other
n -> odd || even
same && odd(n >= 1) -> l
same && even -> -1
other && odd -> 
other -> even (>= 4) -> l l l ..... bsb bsb
int main()
{
	std::ios::sync_with_stdio(false);
	std::cin.tie(0);
	std::cout.tie(0);
	int t;
	std::cin >> t;
	while (t--)
	{
		solve();
	}
	__Made in France__
}
